# Postgres — provision via `vercel dashboard` / marketplace, or point at local Postgres for dev.
DATABASE_URL=postgres://user:password@localhost:5432/forklore

# Vercel Blob — stores each novel's packed git repo bundle.
BLOB_READ_WRITE_TOKEN=

# Auth.js
AUTH_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=

# AI writing assistance (Vercel AI Gateway)
AI_GATEWAY_API_KEY=
